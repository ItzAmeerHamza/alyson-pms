# Python dependencies for desktop agent input monitoring
# These are bundled with the app to ensure cross-platform compatibility

# macOS Quartz framework for low-level input detection
pyobjc-framework-Quartz>=9.0; sys_platform == 'darwin'
pyobjc-framework-Cocoa>=9.0; sys_platform == 'darwin'
pyobjc-core>=9.0; sys_platform == 'darwin'

# Windows dependencies (if needed in future)
# pywin32>=305; sys_platform == 'win32'

# Linux dependencies (if needed in future)
# evdev>=1.6.1; sys_platform == 'linux'

