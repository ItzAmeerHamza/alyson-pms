# Alyson Pulse

See [README.md](../README.md) for architecture and [backend/API.md](../backend/API.md) for REST endpoints.

## AWS RDS

Use `db/schema.sql` for new databases. Connection via `DATABASE_URL` or `DATABASE_HOST` + `DATABASE_PASSWORD` in the API Lambda.

## Lambda deployment

See [infra/sam/README.md](../infra/sam/README.md).
